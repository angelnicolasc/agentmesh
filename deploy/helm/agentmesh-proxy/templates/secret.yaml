{{- if and .Values.config.apiKey (not .Values.config.existingSecret) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "agentmesh-proxy.fullname" . }}-secret
  labels:
    {{- include "agentmesh-proxy.labels" . | nindent 4 }}
type: Opaque
data:
  AGENTMESH_API_KEY: {{ .Values.config.apiKey | b64enc | quote }}
{{- end }}
