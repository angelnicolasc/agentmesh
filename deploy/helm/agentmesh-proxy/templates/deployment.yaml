apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "agentmesh-proxy.fullname" . }}
  labels:
    {{- include "agentmesh-proxy.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "agentmesh-proxy.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "agentmesh-proxy.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
              protocol: TCP
          env:
            - name: AGENTMESH_PROXY_PORT
              value: {{ .Values.service.port | quote }}
            - name: AGENTMESH_ENDPOINT
              value: {{ .Values.config.endpoint | quote }}
            - name: AGENTMESH_GOVERNANCE_LEVEL
              value: {{ .Values.config.governanceLevel | quote }}
            {{- if .Values.config.existingSecret }}
            - name: AGENTMESH_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.config.existingSecret }}
                  key: {{ .Values.config.existingSecretKey }}
            {{- else if .Values.config.apiKey }}
            - name: AGENTMESH_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "agentmesh-proxy.fullname" . }}-secret
                  key: AGENTMESH_API_KEY
            {{- end }}
          {{- if .Values.agentmeshConfig }}
          volumeMounts:
            - name: config
              mountPath: /app/.agentmesh.yaml
              subPath: .agentmesh.yaml
              readOnly: true
          {{- end }}
          livenessProbe:
            {{- toYaml .Values.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.readinessProbe | nindent 12 }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      {{- if .Values.agentmeshConfig }}
      volumes:
        - name: config
          configMap:
            name: {{ include "agentmesh-proxy.fullname" . }}-config
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
